general:
  artifacts:
    - "app/build/outputs/apk"
test:
  override:
    - ./gradlew assembleDebug
deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync app/build/outputs/apk s3://apkdeploy/apk --delete --region ap-northeast-2